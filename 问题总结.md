##### 列表ui如何将如库存格子自动排列问题

使用Grid Group

![image-20251217185408416](P:\GameProject\Unity\EroChara\MD\assets\image-20251217185408416.png)

可以把子类的所有组件按参数排列



##### 鼠标操控角色的部位交互

使用ScreenPointToRay

> `ScreenPointToRay()` 是Unity中`Camera`类的一个方法，用于将屏幕上的一个点转换为一条射线。这条射线的起点是摄像机在屏幕上对应的点，方向是从摄像机出发指向那个点。这在进行射线命中检测时非常有用，特别是与用户界面和鼠标交互相关的场景中。

```c
     Ray ray = Camera.main.ScreenPointToRay(Input.mousePosition);
        RaycastHit hit;

        if (Physics.Raycast(ray, out hit, 100f))
        {
            if(hit.collider.CompareTag("Player"))
                Debug.Log("碰到头了");
            else if(hit.collider.CompareTag("Hand"))
                Debug.Log("碰到手了");
        }

```

